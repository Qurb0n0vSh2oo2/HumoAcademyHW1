package pkg

import "bankCLI/pkg/models"

// var Database = make(map[string]float64)

var Clients = make([]*models.Clients, 0)

var Cities = make([]models.City, 0)

var Transfer = make([]models.Transfer, 0)

var Percent = 10.0
